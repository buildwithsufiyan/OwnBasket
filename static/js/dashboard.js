(function(){'use strict';var toggle=document.querySelector('[data-account-toggle]');var sidebar=document.getElementById('account-sidebar');if(toggle&&sidebar){toggle.addEventListener('click',function(){var open=sidebar.classList.toggle('is-open');toggle.setAttribute('aria-expanded',String(open));});}
var password=document.querySelector('[data-password-form] input[name="new_password1"]');if(password){var meter=document.querySelector('.password-strength');var label=document.querySelector('[data-strength-label]');password.addEventListener('input',function(){var value=password.value;var score=0;if(value.length>=8)score++;if(/[a-z]/.test(value)&&/[A-Z]/.test(value))score++;if(/\d/.test(value))score++;if(/[^A-Za-z0-9]/.test(value))score++;meter.dataset.score=score;meter.querySelector('span').style.width=(score*25)+'%';label.textContent=value?(score<2?'Weak password':score<4?'Good password':'Strong password'):'Use at least 8 characters with a mix of character types.';});}}());
